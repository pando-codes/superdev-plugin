{
  "name": "superdev",
  "description": "Turn ideas into working code through a brainstorm, plan, execute, and evaluate workflow, and let agents pull well-defined work from the pando-catalog and push updates back. Role-scoped, leased, and safe for several agents at once. Follows the principles of ATDD and the Agile SDLC.",
  "version": "0.5.1",
  "author": {
    "name": "Alex",
    "email": "alex@pando.codes"
  },
  "mcpServers": {
    "pando-catalog": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/dist/stdio.js"
      ],
      "env": {
        "PANDO_CATALOG_API_URL": "${PANDO_CATALOG_API_URL}",
        "PANDO_CATALOG_API_KEY": "${PANDO_CATALOG_API_KEY}",
        "SUPERDEV_ROLE": "${SUPERDEV_ROLE}",
        "SUPERDEV_AGENT_ID": "${SUPERDEV_AGENT_ID}",
        "SUPERDEV_CONFIG": "${SUPERDEV_CONFIG}",
        "SUPERDEV_HOME": "${SUPERDEV_HOME}",
        "CLAUDE_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
